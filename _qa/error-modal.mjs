import {chromium} from 'playwright';import assert from 'node:assert/strict';const b=await chromium.launch({executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome',headless:true});try{const p=await b.newPage();await p.goto('http://127.0.0.1:5226/');await p.evaluate(()=>window.addEventListener('pagehide',()=>alteruLocalStorage.setItem('before-the-close','{broken')));await p.reload();await p.keyboard.press('Escape');assert.ok(await p.getByText('暂时无法读取旧进度',{exact:true}).isVisible());await p.getByRole('button',{name:'保留备份，开始新调查'}).click();assert.ok(await p.getByRole('button',{name:'拿起访客证，开始调查'}).isVisible());console.log('corrupt save: Escape preserved recovery; backed up and reopened')}finally{await b.close()}
