# 第二轮视觉签收（2026-09-21）
旧签收曾因动作、世界设定和整体美术问题撤销。现在以新生成的纽约人物、四套功能场景和两尺寸真实运行证据重新签收；历史候选仍不恢复准入。

## 当前运行素材
- 主角：分方向平台帧装配为 3×4 图集，左右使用整帧镜像；四向静止与连续步态在真实地图复验，状态 `accepted`。
- NPC 与头像：旧 v2 四宫格 NPC 被用户撤销准入，原因是与已认可主角相比轮廓更小、头身和像素表现不一致。五人现均以主角的前/侧/后方向单图为直接平台参考重新生成，采用同一透明化与图集装配；运行时按感知比例等比补偿至 114/128 格高。五组主角相邻同屏截图已复验，状态 `accepted`。缩略头像和放大头像继续使用既有平台任务。
- 场景：Northline、RelayOps workspace、data room、Harbor & Pine 分别使用独立地面和功能家具；家具独立碰撞与落地深度，390×844 / 320×568 复验，状态 `accepted`。
- 互动落点：家具重排后完整行走发现两处 P0，已修复为消费清单内安全落点并增加互动半径测试；两尺寸重新通关通过。

# 素材准入
## 主角
首轮全图集方向失败；第二轮侧向站姿失败；最终分方向 v3 加独立侧站姿，整帧镜像右向。12格统一头部锚点与脚底，不修改局部肢体。透明化只清理连通背景和一像素边缘。最终 v3 图集经过实际地图两尺寸四向连续帧与静帧复查，agent 视觉准入 accepted。后补 v4 未改善摆臂，单姿势 a/b 比例与步相不一致，未进入运行素材。正面摆臂稍宽留为非阻断风格优化。
## 家具与建筑
平台家具第一批为45度斜视正投，主桌边缘保持平行，按88世界像素宽与80人物框试装。地面独立四种纹理，墙面另图；没有使用整房背景。首轮同屏桌子偏大、地砖边缘明显，已修正后复拍。
## NPC 演员组
旧 NPC 虽与主角使用相同运行显示框，但窄轮廓、较小头部和不同批量生成方式造成明显感知缩小，证明“可见高度误差合格”不足以判定风格一致。新版本不再从四宫格裁切，五人各自使用主角已认可的 down/right/up 单帧作为对应方向编辑参考；左向只做整帧镜像。专项证据为 `_qa/ui/platform-layout-npc-scale-*-390x844.png`，逐人覆盖相同镜头倍率和相邻站位。Daniel 的独立走路候选仍出现过长腿和步相重复，未进入运行图集；当前巡游沿用统一站姿帧，保持角色尺度和身份稳定。
## 声音
文件来自平台；door.mp3 是 door-sound.mp3 的同字节运行副本。生成与下载成功不等于完成听感验收。音量保守、首次手势解锁、后台暂停、全局静音、按实际位移脚步。机械与听感状态分别记录。已测真实解码与时间推进，真人全曲试听未完成，不能记为听感 accepted。
