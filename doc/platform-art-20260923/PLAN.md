# 新平台接口独立重制

继续纽约 VC/PE 的 Before the Close；现有 RPGJS、剧情、UI、交互保留。纯文本初图与本轮派生分别记录，不读取历史游戏图片或旧截图作生成参考。上一轮内置工具素材保留但不冒充本接口生成。

先主角身份站姿、门叶、独立厚墙，再完整动作与样本房。人物按技能逐方向完整姿态；只允许同轮生成的父图作为edit输入并记录。先从 opaque 纯色底测试透明残留问题，去底/等比裁切属于装配且需记录，不能计为模型原生透明输出。

模型选择Sunburst，high；根图不带参考。每批最多3件，遵守4请求/分钟，失败不自动重试或回退模型。每件保留原始request/task/URL/SHA/时延和准入记录。

验收：孤图→等比世界比例→真实RPGJS 390×844/320×568→门往返/移动/碰撞/遮挡。人物未通过完整步态，不能称高保真完整。动态生成能力另验房间骨架先可玩、素材逐件替换和存档恢复，不能从离线作者生成直接推导完成。
