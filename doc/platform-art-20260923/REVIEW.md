# 新平台美术重制评审（2026-09-23，r8）

## 交付结果

四房间、主角、五位NPC、五张肖像和封面已接入默认游戏。保留RPGJS、剧情、地图、档案放大、音乐、系统语言、靠近再操作与存档逻辑。原始主角背向hero-up-a/b得到用户认可，本次保留；不再把鞋口浅色当作既定错误。

本轮共81次生成：text 32、edit 49；均经统一媒体服务指定gpt-image-2.5-sunburst，全部edit只使用同轮新图。来源审计无历史图依赖；请求耗时中位数34.1秒。这个数字不是整游戏生产耗时、并发容量或首轮合格率。运行包覆盖65个图片文件，包含派生图层和缩略图，不等于65张独立生成。

## 修正与保留的失败证据

- 主角初版过长、第二版过度俯视；最终选第三版身份。九源图装配十二格，右向采用整帧镜像。背向重新尝试中的部分B复制A，未进入最终图集；最终恢复用户认可的原始A/B。
- 分析员完整十二源姿态，携带文件夹，左右分别生成。首次左向B重复A，换根图重生；站姿过长也纠正。其他四位NPC本来不巡游，仅三方向站立加右向整帧镜像，不冒充全员行走图集。
- 主家具正投轴向；两件柜子首版顶面收缩，使用明确二维矩形顶面重生后接入。45度扶手椅只有一件，其余主家具轴向。
- 地板、墙、门、物件分别生成。北墙在侧墙后，南墙盖住侧墙。门近端框独立排序；门洞端面由同轮墙材质裁块装配，不是模型原生整套门洞。
- 桌柜/推车同时限高限宽，碰撞另取实际落地部分。新增办公椅丰富工作区，所有交互接近点重新验路。
- 纯色底显式去底，清理深色洋红边缘并检查紫色衣物保留；不声称原生透明输出。肖像和封面不做颜色键处理。
- 截图脚本因未固定locale而失败时曾误读旧目录截图，已明确告知用户并改独立目录，校验实际engine图片路径；旧图不作为本轮证据。

## 验证

- 17项测试通过。新增房间布局与真实运行使用同一函数，覆盖门口/交互点可达、占地阻挡和透明角通行。
- 主角两尺寸四方向，每方向117–121世界单位，超过两个52单位步态循环；切帧随实际位移，停下回站。见evidence/platform-gait.json。机械数据证明动画执行；视觉自然度仍允许用户试玩复核。
- 分析员两尺寸左右巡游44单位，左右完整三姿态出现；靠近停止、面对玩家。见evidence/platform-npc.json。
- 正式dist两尺寸完成收集、全部NPC对话、资料展开、头像放大、三项证据判断、两条谈判分支、结局、刷新恢复，零页面异常。见evidence/dist-playthrough.log。
- 全房间两尺寸截图在本地 `_qa/ui/platform-world/`；正式构建剔除旧运行图片，源码旧图仍保留回退。公开凭据扫描与API base检查通过。

## 发布与能力边界

发布结果以doc/release.json为准。默认新素材位于public/art/platform-v1，构建对应dist/art/platform-v1。原始生成文件及prepared候选留在本地试验目录，公开源码保存请求/哈希、计划、运行图片和装配脚本；需要恢复原图时脚本下载现有响应URL，不重新生成。

作者侧新接口制作已经跑通；没有据此宣称平台玩家动态生成房间、陌生agent独立复现或统计稳定率验收通过。动态能力还需虚空房先可玩、逐件替换、失败恢复与存档的专项验证。
